# space/

Formulation search space definitions. `FormulationSpace` maps a dataset's lipid components and molar ratios into the parameter types the optimizer searches over: continuous ratio bounds, discrete lipid identities, and simplex-constrained mixture compositions. Used by the CLI and continuous GP-BO pipeline.
